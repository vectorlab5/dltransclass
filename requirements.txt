# Core
torch>=2.2.0,<2.6
transformers>=4.41,<4.50
tokenizers>=0.19
sentencepiece>=0.2
numpy>=1.26,<3.0
scipy>=1.11
scikit-learn>=1.4

# Data and IO
pandas>=2.1
pyarrow>=15.0
datasets>=2.18
PyYAML>=6.0
tqdm>=4.66

# Optimal transport (used for verification of barycenter math; the closed-form
# Dirac case used at training time does not require this dependency)
POT>=0.9

# Visualization
matplotlib>=3.8
seaborn>=0.13
umap-learn>=0.5
plotly>=5.20

# Logging and experiment tracking
tensorboard>=2.16
wandb>=0.17

# Reproducibility helpers
hydra-core>=1.3
omegaconf>=2.3

# Optional baselines
mamba-ssm>=2.0       ; platform_system != "Darwin"
peft>=0.11           # for LongLoRA-style adapter baselines
