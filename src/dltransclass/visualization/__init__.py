"""Visualization utilities (t-SNE, field-attribution, latent-space plotting).

These are kept in a separate module so they can be imported lazily without
forcing matplotlib/seaborn into the runtime path of ``dltransclass.train``.
"""
